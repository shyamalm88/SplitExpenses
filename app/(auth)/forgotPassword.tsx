import React, { memo } from "react";
import { ForgotPasswordScreen, RegisterScreen } from "../Screens";

const ForgotPasswordLayout = () => {
  return <ForgotPasswordScreen />;
};

export default memo(ForgotPasswordLayout);
